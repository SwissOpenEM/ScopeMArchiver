{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start RabbitMQ",
      "command": "docker", // Could be any other shell command
      "args": [
        "run",
        "-d",
        "-p",
        "5672:5672",
        "rabbitmq"
      ],
      "type": "shell",
    }
  ]
}